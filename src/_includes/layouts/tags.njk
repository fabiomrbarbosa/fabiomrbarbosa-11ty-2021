---
permalink: /{{ subDir }}tags/
layout: layouts/base.njk
translationKey: tags
---
{% Heading {
  title: 'Tags'
} %}

{% ArchiveList {
  className: 'tags--archive'
} %}

  <div class="taglist">
  {% for tag in collections.tagList %}
    {% set tagUrl %}/{{ subDir }}tags/{{ tag | slug }}/{% endset %}
    <a href="{{ tagUrl }}" class="tag">{{ tag }}</a>
  {% endfor %}
  </div>

{% endArchiveList %}
