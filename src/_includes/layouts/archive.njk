---
layout: layouts/base.njk
templateClass: tmpl-archive
---
{% Heading {
  title: dictionary.archive.title[ locale ],
  lede: dictionary.archive.subtitle[ locale ],
  className: 'heading--archive'
} %}

{% ArchiveList {
  className: 'feed'
} %}

  {% set postlist = collections[locale] | includes('data.contentType', 'article') %}
  {%- include "partials/postlist.njk" -%}

{% endArchiveList %}

{%- include "partials/subscribe.njk" -%}
