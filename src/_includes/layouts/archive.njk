---
layout: layouts/base.njk
templateClass: tmpl-archive
---
{%- from "components/init.njk" import component -%}
{{ component('heading', {
  title: title,
  lede: lede,
  className: 'heading--archive'
}) }}

{% ArchiveList {
  className: 'feed block'
} %}

  {% set postlist = collections[locale] | includes('data.contentType', subContentType) %}
  {%- include "partials/postlist.njk" -%}

{% endArchiveList %}

{%- include "partials/subscribe.njk" -%}
